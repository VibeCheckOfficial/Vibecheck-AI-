import { defineConfig } from 'tsup';

export default defineConfig({
  entry: ['src/index.ts'],
  format: ['esm'],
  dts: true,
  clean: true,
  sourcemap: true,
  target: 'node20',
  // Bundle dependencies to avoid runtime resolution issues
  noExternal: ['@repo/shared-types'],
});
